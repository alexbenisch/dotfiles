\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}

% Full-width longtables (pandoc default for markdown tables)
\setlength{\LTleft}{0pt}
\setlength{\LTright}{0pt}

% Arrows and similar symbols are missing from Noto Sans; map them to math
\usepackage{newunicodechar}
\newunicodechar{→}{\ensuremath{\rightarrow}}
\newunicodechar{←}{\ensuremath{\leftarrow}}

% Horizontal rules span full text width
\let\oldrule\rule
\renewcommand{\rule}[2]{\noindent\oldrule{\linewidth}{#2}}
